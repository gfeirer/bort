<%%= title "<%= singular_name.humanize %>" %>

<% for attribute in attributes -%>
  <div class="show_group">
    <span class="attribute"><%= attribute.column.human_name %>:</span>
    <span class="value"><%%=h @<%= singular_name %>.<%= attribute.name %> %></span>
  </div>

<% end -%>

<%%= link_to 'Editar', edit_<%= singular_name %>_path(@<%= singular_name %>) %>&nbsp;|&nbsp;<%%= link_to 'Atrás', :back %>
